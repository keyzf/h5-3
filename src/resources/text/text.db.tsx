/**
 * 模板
 * @param index
 */
export const $$horizontal_text = (index): object => {
  const common = {
    type: 'text',
    name: 'ex_h_text',
  };
  const base = {
    html: `<p style="text-align:center;font-size: 25px">横排文本示例</p>
   <p style="text-align:center;font-size: 14px">点左侧组件可以添加新内容</p>
   <p style="text-align:center;font-size: 14px">点一下可以进行删除等操作</p>
 `,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 206,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$vertical_text = (index): object => {
  const common = {
    type: 'text',
    name: 'ex_v_text',
  };
  const base = {
    html: `<p style="text-align:center;font-size: 25px">竖排文本示例</p><p style="text-align:center;font-size: 20px">元日</p><p style="text-align:center;font-size: 14px">爆竹声中一岁除<br/>春风送暖入屠苏<br/>千门万户曈曈日<br/>总把新桃换旧符</p><br/><br/>`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 206,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};

/**
 * 标题
 */
export const $$title_1_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_1_text',
  };
  const base = {
    html: `请输入标题`,
    index: index,
    color: 'rgb(0,161,159)',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_2_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_2_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: 'rgb(0,214,205)',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_3_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_3_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: 'rgb(0,214,205)',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };
  return { common, base, position };
};
export const $$title_4_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_4_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_5_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_5_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_6_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_6_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_7_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_7_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_8_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_8_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_9_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_9_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_10_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_10_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_11_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_11_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_12_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_12_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_13_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_13_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_14_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_14_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_15_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_15_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_16_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_16_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_17_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_17_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$title_18_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'title_18_text',
  };
  const base = {
    html: `你看到的是一个标题`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 50,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};

/**
 *  段落
 */
export const $$paragraph_1_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_1_text',
  };
  const base = {
    html: `天堑无涯，绿杨陌上多离别，墙花路人暗想浮生`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_2_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_2_text',
  };
  const base = {
    html: `正如年年岁岁花相似，岁岁年年人不同。只道是不悔梦归处，只恨太匆匆。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_3_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_3_text',
  };
  const base = {
    html: `乌云蔽月，人迹踪绝，说不出如斯寂寞。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_4_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_4_text',
  };
  const base = {
    html: `一身覆雪，雪洗风尘。凝视旷野，雪落无言。心灵沉静如冬`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_5_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_5_text',
  };
  const base = {
    html: `从生命的开始，见到第一缕阳光，你就一直在我身旁，和我一起成长，一起历经人生的酸甜苦辣，有时颓废，有时忧伤，你都沉默的拍拍我的肩膀；有时快乐，有时幸福，你都会飞扬的随着我手舞足蹈。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_6_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_6_text',
  };
  const base = {
    html: `开满朝颜的晚秋，我可以卸掉一身倦怠，独自踏上旅途。趁晚秋未晚，我伴疏雨，轻车铁轨，我临窗静候，看车窗物景移变，幻出迷离异芒，织成一道道流光，弹落我心蒙蒙咸埃……唯有静候。晚秋。疏雨。入梦而来。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_7_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_7_text',
  };
  const base = {
    html: `人世间有太多的情感与归宿我们不能把握，构成了命运的不确定与爱情的不可求。在这个浮华掠影幻梦破灭的年代，对于爱情、事业、青春与梦想，我们通常只能默然相守，寂静求欢。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_8_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_8_text',
  };
  const base = {
    html: `说谎和沉默可以说是现在人类社会里日渐蔓延的两大罪恶。事实上，我们经常说谎，动不动就沉默不语。——村上春树《且听风吟》`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_9_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_9_text',
  };
  const base = {
    html: `站在高高的神坛上，俯瞰人生，没看到世间其乐融融，只看到哀思遍地，忧伤漫天，美丽如雪般凋零，淡漠如烟般飘散。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_10_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_10_text',
  };
  const base = {
    html: `转身，一缕冷香远，逝雪深，笑意浅。来世你渡我，可愿？`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_11_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_11_text',
  };
  const base = {
    html: `我曾听人说过，当你不能够再拥有，你唯一可以做的，就是令自己不要忘记。——《东邪西毒》`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_12_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_12_text',
  };
  const base = {
    html: `我微笑着看你来的方向，并不意味着我快乐，不眠夜，馨香若兰的气息，是对影似水的柔情`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_13_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_13_text',
  };
  const base = {
    html: `真切的生活，真心的笑容，真挚的情感，真情的流露。面对真实，我无须掩饰。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_14_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_14_text',
  };
  const base = {
    html: `秋夜冷峻云烟，渐次消褪，淡淡的月色，透过密集摇叶的缝隙，洒下斑驳的光点。摇曳着月影的婆娑，忽明忽暗，遥指苍穹，蓝色的天幕上散落着点点寒星。月色在灵动的云层间游弋，忽隐忽现、瞬息万变，呈现出无限的遐想..`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_15_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_15_text',
  };
  const base = {
    html: `夜未央，流星落，情已殇。爱在夜色里永远是传奇`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_16_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_16_text',
  };
  const base = {
    html: `天堑无涯，绿杨陌上多离别，墙花路人暗想浮生。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_17_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_17_text',
  };
  const base = {
    html: `一身覆雪，雪洗风尘。凝视旷野，雪落无言。心灵沉静如冬。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};
export const $$paragraph_18_text = (index): { common; base; position } => {
  const common = {
    type: 'text',
    name: 'paragraph_18_text',
  };
  const base = {
    html: ` 染火枫林，琼壶歌月，长歌倚楼。岁岁年年，花前月下，一尊芳酒。水落红莲，唯闻玉磬，但此情依旧。`,
    index: index,
    color: '',
  };
  const position = {
    width: 300,
    height: 282,
    left: 0,
    top: 0,
    rotate: 0,
    zIndex: 100,
  };

  return { common, base, position };
};

