import { Map } from "immutable";

export const $$background_database = Map(
  {
    name: "background",
    customize: Map({
      img:{
        upload: { value: "" }
      },
      img_config: {},
      color: ''
    }),
  }
);

